include: configs/base.yaml
training:
  resume_checkpoint: outputs/checkpoints/best_model.pt
  checkpoint_dir: outputs/checkpoints_finetuned
  log_dir: outputs/logs_finetuned
  epochs: 80
  lr: 0.00005
  freeze_encoder_processor: false
fine_tune:
  dataset_ids: []
  # full | decoder_only | processor_decoder
  mode: full
  compare_before_after: true
