include: configs/base.yaml

model:
  latent_dim: 16
  message_passing_steps: 1
  mlp_layers: 2
  dropout: 0.0

training:
  epochs: 1
  lr: 0.0003
  checkpoint_dir: outputs/smoke_checkpoints
  log_dir: outputs/smoke_logs

# Use only for code/dimension checks. For real training use configs/base.yaml.
